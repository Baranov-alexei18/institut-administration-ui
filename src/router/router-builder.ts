import { ROUTES } from '../constants/routes-path';

export const routesBuilder = {
  home: () => `/${ROUTES.home}`,

  about: () => `/${ROUTES.about}`,

  registration: () => `/${ROUTES.registration}`,
};
